# 인코더 추론: 데모에서 출시까지

브라우저(WASM/WebGPU)에서 돌리던 int8 인코더를 서버 측 CUDA(A100)로 옮겨 데모를 보여준 뒤, 실제 출시까지 갈 때 고려할 것들을 정리한 문서.

---

## 0. 현재 상황 요약

- 브라우저에서 int8 인코더를 WASM / WebGPU로 벤치마크함
- 같은 기기 기준 **WASM(1392ms) > WebGPU(1567ms)** — int8 연산을 WebGPU가 지원 못 해 일부 op이 CPU로 떨어지고, GPU↔CPU 전송 오버헤드까지 붙어 오히려 느려짐
- 데모는 서버의 A100 + CUDA 추론으로 보여줄 예정
- WebGPU는 브라우저용 기술이라 헤드리스 데이터센터 GPU(A100)에선 사실상 못 씀 → 서버에선 CUDA 경로가 정답

---

## 1. 데모 단계 (지금)

목표: "동작한다 + 빠르다"를 보여주는 것. 완성도보다 시연 성공률이 중요.

- **ONNX Runtime + CUDAExecutionProvider** + FastAPI 정도면 충분
- 모델은 일단 가진 int8 .onnx 그대로 사용 (재최적화는 나중에)
- 단일 인스턴스, 단일 사용자 가정
- 에러 핸들링은 최소한만, 로깅은 콘솔 수준
- **데모 전 체크**: 콜드 스타트(첫 요청이 느림) 대비해서 시연 직전 워밍업 1~2회 돌려두기

⚠️ 데모에서 잘 돌아간다고 출시 준비가 된 게 아니다. 아래 항목들이 데모와 출시를 가르는 지점.

---

## 2. 모델 최적화

데모에선 미뤘지만 출시 전 반드시 다시 볼 부분.

| 항목 | 내용 |
|------|------|
| **정밀도 재검토** | 브라우저에서 int8을 쓴 건 모델 크기·CPU 호환성 때문. A100에선 fp16/bf16이 더 빠르고 정확도도 좋을 수 있음. 서버에선 int8을 고집할 이유가 없을 수도 있다 |
| **TensorRT 전환** | 극한 속도가 필요하면 ONNX Runtime → TensorRT. A100의 int8/fp16 텐서코어를 제대로 활용 |
| **정확도 검증** | 양자화/정밀도 바꿀 때마다 출력 임베딩이 기존과 얼마나 차이 나는지 회귀 테스트 필요 |
| **모델 버전 관리** | 어떤 모델 파일이 어떤 커밋·정확도와 매칭되는지 추적 |

---

## 3. 성능 & 확장성

| 항목 | 내용 |
|------|------|
| **배칭(dynamic batching)** | A100의 진짜 강점은 여러 요청을 묶어 한 번에 처리할 때. 사용자가 많으면 필수 |
| **동시성/큐잉** | 요청이 몰릴 때 GPU 메모리 초과·OOM 방지. 요청 큐와 동시 처리 한도 설정 |
| **Triton Inference Server** | 배칭·동시성·모델 버저닝을 자동 처리. 트래픽이 커지면 직접 짜는 것보다 유리 |
| **오토스케일링** | 트래픽에 따라 GPU 인스턴스 수 조절. A100은 비싸므로 유휴 시간 비용 관리가 중요 |
| **콜드 스타트** | 모델 로딩에 수 초~수십 초. 인스턴스 워밍업 전략 필요 |

---

## 4. 레이턴시 vs 네트워크 (실시간성)

`/realtime`이라는 이름 = 실시간성이 핵심. 브라우저 추론과 달리 서버 추론은 **네트워크 왕복 비용이 새로 생긴다.**

- 이미지가 크면 **업로드+다운로드 시간 > 추론 시간**일 수 있음 → 이 경우 서버 이전이 손해일 수도
- 대응책:
  - 클라이언트에서 이미지 리사이즈/압축 후 전송
  - 지역별 엣지 배포 / CDN 고려
  - WebSocket·스트리밍으로 연결 오버헤드 줄이기
- **트레이드오프 결정**: "빠른 단건(레이턴시)" vs "많은 처리량(throughput)" 중 무엇이 우선인지 먼저 정의

---

## 5. 안정성 & 운영

| 항목 | 내용 |
|------|------|
| **에러 핸들링** | 잘못된 입력, 모델 실패, GPU OOM, 타임아웃에 대한 graceful 처리 |
| **헬스체크** | GPU 가용성·모델 로드 상태 체크 엔드포인트 |
| **모니터링** | 레이턴시(p50/p95/p99), 처리량, GPU 사용률·메모리, 에러율 |
| **로깅** | 구조화된 로그 + 요청 추적 ID |
| **그레이스풀 셧다운** | 배포·재시작 시 진행 중 요청 보호 |
| **재현성** | 드라이버·CUDA·라이브러리 버전 고정 (Docker 이미지로 박제) |

---

## 6. 보안 & 권한

| 항목 | 내용 |
|------|------|
| **인증/인가** | 추론 API 무방비 노출 금지. API 키 또는 토큰 |
| **Rate limiting** | 남용·DoS 방지. A100은 비싸므로 특히 중요 |
| **입력 검증** | 악성 파일·과대 이미지 차단 |
| **데이터 프라이버시** | 사용자 이미지를 서버에서 처리 → 저장 정책·암호화·삭제 정책 명시. (브라우저 추론 땐 데이터가 기기를 안 떠났다는 점이 오히려 장점이었음 — 이 변화를 의식할 것) |

---

## 7. 비용

A100은 시간당 비용이 큼. 출시 전 반드시 계산.

- **GPU 가동 시간** = 가장 큰 비용. 유휴 인스턴스 최소화
- **인스턴스 타입 검토**: A100이 과한지? 더 작은 GPU(L4, T4 등)로 충분한지 부하 테스트로 확인
- **요청당 단가** 추정 → 가격 정책·한도 설계의 근거
- **하이브리드 고려**: 가벼운 케이스는 클라이언트(브라우저)에서, 무거운 것만 서버로 — 비용·레이턴시 모두 절약 가능

---

## 8. 배포 & 인프라

| 항목 | 내용 |
|------|------|
| **컨테이너화** | CUDA·드라이버 포함한 Docker 이미지. 환경 재현성 확보 |
| **CI/CD** | 모델·코드 배포 파이프라인, 정확도 회귀 테스트 자동화 |
| **카나리/블루-그린** | 새 모델·코드 점진 배포로 리스크 축소 |
| **롤백 전략** | 문제 시 이전 모델·버전으로 즉시 복귀 |
| **IaC** | 인프라를 코드로 관리(Terraform 등) |

---

## 9. 의사결정이 필요한 핵심 갈림길

출시 설계 전에 먼저 답해야 할 질문들:

1. **단일 사용자 vs 다중 사용자** → 후자면 배칭·Triton·오토스케일링이 전부 필요해짐
2. **레이턴시 우선 vs 처리량 우선** → 실시간이면 네트워크 비용이 최대 관건
3. **int8 유지 vs fp16/TensorRT 전환** → 서버에선 정밀도 재검토 가치 큼
4. **순수 서버 추론 vs 하이브리드(클라+서버)** → 비용·프라이버시·레이턴시에 직결
5. **예상 트래픽 규모** → 모든 인프라·비용 결정의 출발점

---

## 빠른 요약

- **데모**: ONNX Runtime CUDA + FastAPI, int8 그대로, 워밍업만 챙기면 됨
- **출시 전 必**: 모델 정밀도 재검토(fp16/TensorRT), 배칭·동시성, 네트워크 레이턴시, 모니터링, 인증·rate limit, 비용 계산
- **가장 먼저 정할 것**: 트래픽 규모 + 레이턴시 vs 처리량 우선순위
- **놓치기 쉬운 함정**: 서버 이전으로 (1) 네트워크 왕복 비용, (2) 데이터 프라이버시 책임이 새로 생긴다는 점
