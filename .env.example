# ONNX 모델 파일 디렉터리 (호스트 기준, docker에서 /models 로 마운트)
# <repo-경로>는 이 저장소를 clone한 절대경로로 교체하세요 (예: /home/<user>/ki67-service)
ONNX_DIR=<repo-경로>/deployment

# 학습 데이터 루트 디렉터리 (DP_MedificientSAM/.env 에서 실제 값 설정)
# train_npz/ 의 부모 디렉터리를 지정
CVPR2024_MEDSAM_DATA_DIR=<repo-경로>
